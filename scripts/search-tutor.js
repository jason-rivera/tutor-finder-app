
// having default values for rightside of comparison if user does not specify query
// eg. if user doesn't specify rate, use MAX_RATE --> .where("rate", "<=", "MAX_RATE")

// citiesRef.where("state", ">=", "CA").where("state", "<=", "IN");
